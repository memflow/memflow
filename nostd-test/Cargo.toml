[package]
name = "nostd-test"
version = "0.1.0"
authors = ["Aurimas Blažulionis <0x60@pm.me>"]
edition = "2018"

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"

[dependencies]
flow-core = { path = "../flow-core/", default-features = false }
